// Exportaciones de componentes
export { Header } from './components/Header';
export { Footer } from './components/Footer';
export { MobileMenu } from './components/MobileMenu';
export { LegalModal } from './components/LegalModal';

// Exportaciones de hooks
export { useModal } from './hooks/useModal';

// Exportaciones de constantes
export { APP_CONFIG, EXTERNAL_LINKS, ROUTES, SEO_CONFIG, ANALYTICS_CONFIG, SOCIAL_CONFIG } from './constants';

// Exportaciones de tipos
export type { ModalProps, AppConfig, ExternalLinks, Routes } from './types';
